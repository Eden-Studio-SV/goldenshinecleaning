Rounded white surface with soft navy-tinted shadow — the container for service cards, pricing, testimonials.

```jsx
<Card padding="lg">…</Card>
<Card interactive>…clickable service…</Card>
```

`padding`: `none`/`sm`/`md`/`lg`. Set `interactive` for a hover lift on clickable cards. Corners are always generously rounded (20px).
