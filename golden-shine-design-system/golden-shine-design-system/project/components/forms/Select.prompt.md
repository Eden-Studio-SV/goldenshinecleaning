Styled dropdown matching the Input field — for service type, property size, frequency, etc.

```jsx
<Select label="Service type" placeholder="Choose one"
  options={["House cleaning", "Office cleaning", "Deep clean", "Move-out"]} />
<Select label="How often?" options={[{value:'weekly',label:'Weekly'},{value:'biweekly',label:'Every 2 weeks'}]} />
```
