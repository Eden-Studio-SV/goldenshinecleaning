{
  "name": "golden-shine-mail-functions",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": { "node": "22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config vitest.config.ts",
    "test:coverage": "firebase --config ../firebase.json emulators:exec --only firestore --project demo-goldenshine 'vitest run --coverage --config vitest.coverage.config.ts'",
    "test:integration": "firebase --config ../firebase.json emulators:exec --only firestore --project demo-goldenshine 'vitest run --config vitest.integration.config.ts'"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "firebase-functions": "^6.5.0",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.17",
    "@vitest/coverage-v8": "^3.2.7",
    "firebase-tools": "^13.29.1",
    "typescript": "^5.7.2",
    "vitest": "^3.2.7"
  }
}
